# LP-Structure-Grounded Process Supervision for LLM-based Replenishment Optimization Modeling

# 面向库存补货优化的大语言模型结构验证增强过程监督方法

## Abstract

Large language models (LLMs) have shown promising capability in generating optimization models and solver code from natural language descriptions. Existing LLM-for-operations-research methods typically rely on result-level supervision signals, such as whether the generated code is executable, whether the solver returns a feasible or optimal solution, whether the final objective value matches a reference answer, or whether multiple candidates are mutually consistent. However, these signals provide limited insight into whether the generated optimization model contains the correct internal structure. This limitation is particularly critical for inventory replenishment optimization, where domain-specific structures such as inventory balance, order variables, shortage variables, capacity constraints, fixed ordering costs, binary order-trigger variables, and Big-M linking constraints directly determine model semantics.

In this paper, we propose **ReplenishVerifier**, an LP-structure-grounded process supervision framework for LLM-based replenishment optimization modeling. Given solver code generated by an LLM, ReplenishVerifier executes the code, exports the induced linear programming (LP) representation, parses the LP file, and verifies whether the resulting mathematical model satisfies inventory-specific structural rules. The framework combines solver execution feedback with domain-structured LP verification to support candidate selection, structure-aware self-repair prompt generation, and preference-data construction for future lightweight alignment. We further introduce a replenishment-oriented benchmark covering five representative problem families, including single-period newsvendor, single-item multi-period replenishment, shortage-aware replenishment, multi-item capacity-constrained replenishment, and fixed-order-cost replenishment with Big-M constraints. Experiments are designed to compare Direct generation, Best-of-K selection, Solver-Filter, Structure-Only verification, ReplenishVerifier-Full, and ReplenishVerifier-Repair under main, ablation, low-resource, and difficulty-wise settings. Experimental results will be reported after running the full benchmark and LLM candidate generation pipeline. `[TO FILL: main quantitative findings]`

**Keywords:** LLM for optimization modeling; inventory replenishment; LP structure verification; process supervision; solver-in-the-loop learning; operations research.

---

## 1. Introduction

Optimization modeling is a central task in operations research (OR). Given a decision problem described in natural language, a modeler must identify decision variables, formulate objectives, impose constraints, and implement the resulting mathematical program using a solver interface such as PuLP, Pyomo, Gurobi, or COPT. Recent LLM-for-OR systems, including optimization modeling agents and solver-code generation frameworks, have shown that large language models (LLMs) can automate part of this workflow and can be improved with solver execution, debugging, or verifiable rewards.

However, LLM-based optimization modeling remains fragile. A generated solver program may be syntactically executable while still representing the wrong mathematical model. For example, in an inventory replenishment problem, a model may include an order quantity variable and produce an optimal solver status, yet omit the inventory balance equation. Similarly, a fixed-order-cost replenishment model may contain a binary variable but fail to link the continuous order quantity to the binary order-trigger variable through a Big-M constraint. Such errors are not merely formatting mistakes; they change the semantics of the replenishment decision process.

Existing works provide important foundations but leave a gap for domain-structured verification. OptiMUS-style systems emphasize LLM-based optimization modeling, code generation, and solver debugging. SIRL / SolveOR-R1-style methods use solver execution, objective values, LP artifacts, or verifiable rewards to train or select models. StepORLM / GenPRM-style methods study process supervision or generative process reward modeling for OR reasoning. OptArgus and OptiRepair further study hallucination detection, structural consistency auditing, or repair of optimization models. These directions are closely related to our goal. Nevertheless, most of them are designed as general-purpose modeling, reward, auditing, or repair frameworks. They usually do not explicitly encode the recurring semantic structures of a specific OR domain such as inventory replenishment.

This paper studies the following narrower question: **Can LP artifacts be used to provide replenishment-specific structural evidence for supervising LLM-generated optimization models?** Inventory replenishment is a suitable domain for this question because its correctness depends on recognizable modeling structures, including inventory balance, order variables, shortage or backlog variables, holding and shortage costs, capacity constraints, binary setup variables, fixed ordering costs, and Big-M linking constraints. These structures are intermediate modeling evidence: they are more informative than solver status alone, but more directly verifiable than free-form chain-of-thought reasoning.

We propose **ReplenishVerifier**, an LP-structure-grounded verification framework for LLM-based replenishment optimization modeling. Given LLM-generated solver code, ReplenishVerifier executes the code, exports the induced `.lp` file, parses objective terms, constraints, variables, binary declarations, and bounds, and checks replenishment-specific structural rules. It then converts the resulting structure evidence into candidate-ranking scores, repair feedback, and preference-data signals. ReplenishVerifier is not intended to replace general LLM-for-OR frameworks, solver-based reward methods, or hallucination repair systems. Instead, it is complementary: it supplies domain-specific LP-structure evidence that those systems can use as an additional process supervision signal.

The main contributions of this work are as follows:

1. **A replenishment-specific LP-structure verification perspective.** We identify a gap in existing LLM-for-OR verification: solver execution, objective matching, generic LP statistics, and hallucination auditing do not necessarily reveal whether inventory-specific modeling structures are present. We focus on this domain-structured verification gap rather than proposing a general OR reward model.
2. **The ReplenishVerifier framework.** We implement a modular pipeline that executes generated PuLP code, exports LP artifacts, parses model structure, checks replenishment-specific rules, computes ground-truth-free selection scores, and generates structure-aware repair prompts.
3. **A replenishment-oriented benchmark and structure schema.** We build a benchmark suite covering five representative replenishment families and annotate expected structures such as inventory balance, shortage variables, capacity constraints, binary setup variables, fixed ordering costs, and Big-M constraints.
4. **An experimental protocol for separating structure evidence from solver-only evidence.** We evaluate candidate selection, repair feedback generation, and preference-data construction against Direct, Best-of-K, Solver-Filter, Structure-Only, generic-LP-statistics, and hallucination-detection-style baselines. All formal selection scores avoid using reference objective values; reference objectives are used only for post-selection evaluation.

All empirical values in this draft are intentionally left as placeholders and will be filled after running the full experimental pipeline. `[TO FILL: code/data release link if applicable]`

---

## 2. Related Work

### 2.1 LLMs for Optimization Modeling

Recent studies have explored using LLMs to generate mathematical optimization formulations or solver code from natural language problem statements. OptiMUS-like systems are representative of this direction: they focus on LLM-based optimization modeling, solver-code generation, and debugging of generated programs. These systems show that LLMs can produce useful optimization code and can benefit from iterative execution feedback.

ReplenishVerifier is related but has a narrower and complementary focus. We do not aim to replace general modeling agents or to cover all OR problem classes. Instead, we study a vertical setting—inventory replenishment—and ask whether the LP artifact induced by generated code contains replenishment-specific structures. Thus, our contribution is not another generic code-generation pipeline, but a domain-structured verification layer that can be attached to such pipelines.

### 2.2 Solver-in-the-Loop Verification and Verifiable Rewards

Solver-in-the-loop methods use optimization solvers as external verifiers. Given generated solver code, the system executes the code and observes whether the solver returns an optimal, infeasible, unbounded, or error status. SIRL / SolveOR-R1-style methods further use solver execution, objective values, LP artifacts, or verifiable rewards to train or select models. These approaches provide important result-level and artifact-level supervision signals.

ReplenishVerifier is complementary to this line of work. We also execute solver code and inspect LP artifacts, but the central signal is different. Rather than designing a general reward over OR tasks, we extract replenishment-specific structural evidence from LP files: inventory balance, shortage variables, capacity constraints, binary setup variables, fixed ordering costs, and Big-M links. This means ReplenishVerifier can be used as an additional reward component or diagnostic signal inside solver-in-the-loop learning frameworks.

### 2.3 Process Supervision and Generative PRMs for OR

Process supervision aims to supervise intermediate reasoning or modeling steps rather than only final answers. StepORLM / GenPRM-style work studies process supervision or generative process reward models for OR modeling and reasoning. Such methods are important because optimization modeling often fails in intermediate decisions: variable definition, constraint construction, objective-term inclusion, and indexing.

ReplenishVerifier provides a different kind of process supervision. Instead of asking a model or judge to evaluate free-form reasoning traces, it derives process evidence from the executable LP artifact itself. The presence or absence of an `inventory_balance` constraint, a binary `Y` variable, or a `big_m` linking constraint is a verifiable structural signal. These signals can be converted into scalar scores, repair prompts, or preference pairs. In this sense, ReplenishVerifier can serve as a domain-grounded source of labels for process reward modeling.

### 2.4 Hallucination Detection and Repair in Optimization Modeling

Recent work also studies hallucination detection and repair for optimization models. OptArgus-like systems focus on general hallucination detection, structural consistency auditing, or model-level sanity checks. OptiRepair-like systems study diagnosis and repair of supply-chain optimization models, especially infeasibility or inconsistency repair.

ReplenishVerifier overlaps with these works in that it detects modeling errors and produces repair-oriented feedback. The difference lies in task timing, scope, and output. OptArgus is more general and targets broad hallucination or structural consistency auditing, whereas ReplenishVerifier specializes in inventory replenishment and outputs replenishment-structure scores, candidate rankings, repair prompts, and preference-pair signals. OptiRepair focuses on repair of supply-chain optimization models, especially when models become infeasible or inconsistent. ReplenishVerifier instead operates at LLM generation time: it ranks multiple generated candidates and explains missing replenishment structures even when a candidate is feasible and solved optimally. Therefore, our method should be viewed as complementary to general hallucination auditing and repair systems, not as a replacement.

### 2.5 Inventory Replenishment Optimization

Inventory replenishment is a classical OR domain with rich model structures. Basic variants include the single-period newsvendor model and multi-period replenishment with inventory balance. More advanced variants introduce shortages or backorders, holding and shortage costs, warehouse capacity constraints, fixed ordering costs, binary order decisions, Big-M linking constraints, lead times, service-level constraints, and multi-item or multi-warehouse flows.

These structures make replenishment a suitable testbed for structure-aware LLM verification. Unlike purely generic optimization problems, replenishment tasks have clear domain semantics that can be mapped to LP-level structural patterns. This paper leverages this property to build a vertical benchmark and verification framework.

### 2.6 Positioning Against Closely Related Work

| Line of work | Typical signal / goal | Relation to ReplenishVerifier | Key difference |
|---|---|---|---|
| OptiMUS-style LLM optimization modeling | Generate and debug solver code | Same broad area of LLM-for-OR modeling | We focus on replenishment-specific LP structure verification, not generic modeling/debugging |
| SIRL / SolveOR-R1-style verifiable learning | Solver execution, objective value, LP artifacts, verifiable rewards | Compatible solver-in-the-loop setting | We do not propose a general reward; we extract inventory-specific structural evidence from LP artifacts |
| StepORLM / GenPRM-style process supervision | Process reward models or step-level supervision | Same motivation of supervising intermediate modeling quality | Our process labels are derived from verifiable LP structures rather than free-form reasoning traces |
| OptArgus-style hallucination detection | General hallucination / structural consistency auditing | Related error detection goal | We specialize to replenishment structures and output scores, rankings, repair prompts, and preference signals |
| OptiRepair-style optimization repair | Supply-chain model diagnosis and repair, often infeasibility-focused | Related repair motivation | We operate at LLM generation-time candidate selection and structure feedback, including feasible-but-wrong models |
| ReplenishVerifier | Inventory-specific LP structure checking | — | Domain-structured verification for inventory replenishment models; success still depends partly on naming and rule coverage |

---

## 3. Problem Formulation

### 3.1 LLM-based Replenishment Modeling Task

Let each replenishment problem instance be represented as

\[
x = (d, \theta, s^*),
\]

where \(d\) is a natural language problem description, \(\theta\) denotes structured problem parameters such as demand, costs, capacity, and number of periods/items, and \(s^*\) denotes the expected model structure labels. Given \(d\), an LLM generates a candidate solver program

\[
c = \mathcal{M}(d),
\]

where \(c\) is expected to implement an optimization model using a solver interface such as PuLP.

Executing \(c\) induces an optimization model \(P_c\), solver status \(z_c\), objective value \(v_c\), and optionally an exported LP file \(\ell_c\). A reference implementation provides a reference objective value \(v^*\) and reference structure labels \(s^*\).

The goal is to select or improve candidate \(c\) such that the induced model \(P_c\) is not only executable and numerically correct, but also structurally faithful to the intended replenishment model.

### 3.2 Structure Labels

For each problem instance, ReplenishVerifier defines a binary expected-structure vector:

\[
s^* = (s_1^*, s_2^*, \ldots, s_m^*), \quad s_j^* \in \{0,1\},
\]

where each dimension corresponds to a replenishment modeling structure. The current prototype supports the following structures:

| Structure | Meaning |
|---|---|
| `inventory_balance` | Inventory flow conservation across periods/items |
| `order_variable` | Continuous order quantity variable \(Q\) |
| `inventory_variable` | Inventory state variable \(I\) |
| `shortage_variable` | Shortage/backorder variable \(B\) |
| `capacity_constraint` | Capacity constraint over inventory/order resources |
| `binary_order_variable` | Binary order-trigger variable \(Y\) |
| `big_m_constraint` | Big-M link between \(Q\) and \(Y\) |
| `lead_time` | Lead-time dependent arrival structure |
| `holding_cost` | Holding cost term in objective |
| `shortage_cost` | Shortage penalty term in objective |
| `fixed_order_cost` | Fixed ordering cost term in objective |

Given an exported LP file \(\ell_c\), the verifier parses its internal structure and predicts detected labels:

\[
\hat{s}_c = V(\ell_c), \quad \hat{s}_{c,j} \in \{0,1\}.
\]

### 3.3 Evaluation Metrics

For a selected candidate, we evaluate the following metrics.

**Executable Rate.** The fraction of candidates whose solver code executes without runtime errors and exports a model.

**Feasible/Optimal Rate.** The fraction of candidates for which the solver returns an optimal status. In the current implementation, the primary reported status is `Optimal`.

**Objective Accuracy.** A candidate objective is considered correct if

\[
|v_c - v^*| \le \epsilon_{abs}
\]

when \(|v^*|\) is close to zero, or otherwise

\[
\frac{|v_c - v^*|}{|v^*|} \le \epsilon_{rel},
\]

with \(\epsilon_{abs}=10^{-4}\) and \(\epsilon_{rel}=10^{-4}\).

**Structure Completeness.** For structures expected to be present, the fraction that are detected:

\[
\text{SC}(c) = \frac{\sum_j \mathbb{1}[s_j^*=1 \land \hat{s}_{c,j}=1]}{\sum_j \mathbb{1}[s_j^*=1]}.
\]

**Inventory Balance Accuracy.** For instances requiring inventory balance, the fraction for which inventory balance is detected.

**Constraint Coverage.** Macro-average coverage over all expected structure dimensions.

**Average Runtime.** Average time for candidate execution, LP export, parsing, and verification.

**Average Repair Feedback Count.** Average number of missing-structure feedback items generated per problem.

---

## 4. Method

### 4.1 Overview

ReplenishVerifier is designed as an LP-structure-grounded process supervision framework. The pipeline consists of six stages:

1. **Candidate generation.** An LLM generates one or more solver-code candidates for each natural language replenishment problem.
2. **Solver execution.** Each candidate program is executed using PuLP/CBC. The system records execution status, solver status, objective value, and error messages.
3. **LP export.** If execution succeeds, the induced optimization model is exported to a `.lp` file.
4. **LP structure parsing.** The LP parser extracts objective terms, constraint names, constraint expressions, variable names, binary variables, and bounds.
5. **Replenishment rule checking.** Domain-specific rules check whether expected inventory structures are present.
6. **Structure-grounded scoring and feedback.** The framework computes scores for candidate selection and generates natural language feedback for missing structures.

This process differs from prompt engineering because the supervision signal is not only encoded in prompts. Instead, ReplenishVerifier derives structured verification labels from the actual LP model induced by generated code. Thus, the framework supervises the modeling process through executable model artifacts.

### 4.2 Benchmark Construction

The benchmark is generated from replenishment templates. Each instance contains:

```json
{
  "id": "string",
  "difficulty": "easy|medium|hard|expert",
  "problem_type": "string",
  "natural_language": "string",
  "parameters": {},
  "expected_structures": {},
  "reference_code": "string",
  "reference_objective": 0.0,
  "reference_status": "Optimal",
  "reference_lp_path": "string"
}
```

The current prototype supports five problem families:

1. **single_period_newsvendor.** Single-item single-period model with order quantity, leftover inventory, shortage, holding cost, and shortage cost.
2. **single_item_multi_period.** Single-item multi-period replenishment with inventory balance and holding cost.
3. **single_item_multi_period_shortage.** Multi-period replenishment with shortage/backorder variables and shortage penalty.
4. **multi_item_capacity.** Multi-item multi-period replenishment with capacity constraints.
5. **fixed_order_cost_big_m.** Multi-period replenishment with fixed ordering cost, binary order-trigger variable, and Big-M linking constraint.

Each template generates natural language descriptions, structured parameters, reference PuLP code, reference objective values, LP files, and expected structure labels. The reference code follows consistent naming conventions: order variables use `Q`, inventory variables use `I`, shortage variables use `B`, and binary order-trigger variables use `Y`. Constraint names include `inventory_balance_t`, `capacity_t`, `big_m_t`, and `demand_satisfaction_t` where applicable.

### 4.3 LP Parser

Given a `.lp` file exported by PuLP, the parser extracts:

- optimization sense;
- objective section;
- constraint names;
- constraint expressions;
- variable names;
- binary variables;
- bounds.

The parser is intentionally lightweight and prototype-oriented. It does not implement a full LP grammar. Instead, it targets the LP format emitted by PuLP/CBC and uses robust section-level parsing and regular expressions. This design is sufficient for the current controlled benchmark and makes the verifier easy to extend.

### 4.4 Inventory-Specific Structure Rules

The structure checker maps parsed LP artifacts to replenishment semantics. Examples include:

- `order_variable`: detected if a variable with prefix `Q` exists.
- `inventory_variable`: detected if a variable with prefix `I` exists.
- `shortage_variable`: detected if a variable with prefix `B` exists.
- `binary_order_variable`: detected if a binary variable with prefix `Y` exists.
- `inventory_balance`: detected if constraints are named like `inventory_balance_*` or expressions contain both inventory and order variables in equality constraints.
- `capacity_constraint`: detected if constraint names contain `capacity`.
- `big_m_constraint`: detected if constraint names contain `big_m`, or expressions link `Q` and `Y` through inequality constraints.
- `holding_cost`: detected if inventory variables appear in the objective.
- `shortage_cost`: detected if shortage variables appear in the objective.
- `fixed_order_cost`: detected if binary order-trigger variables appear in the objective.

The output is a structured verification result:

```json
{
  "expected": {},
  "detected": {},
  "passed": {},
  "missing": [],
  "extra_detected": [],
  "structure_score": 0.0,
  "messages": []
}
```

### 4.5 Structure-Grounded Scoring

For each candidate, ReplenishVerifier computes both **selection signals** and **evaluation-only metrics**. This distinction is important for avoiding ground-truth leakage. Formal candidate selection does not use the reference objective value. Instead, the full method uses:

- executable score;
- solver optimality score;
- structure completeness score;
- semantic consistency score.

The full selection score is:

\[
S_{full}(c) = \alpha E(c) + \beta Z(c) + \delta SC(c) + \eta Sem(c),
\]

where \(E(c)\) indicates executability, \(Z(c)\) indicates whether the solver returns an optimal status, \(SC(c)\) is structure completeness, and \(Sem(c)\) is a semantic consistency score derived from missing replenishment structures. In the current implementation, the default weights are:

\[
\alpha=0.25, \quad \beta=0.25, \quad \delta=0.35, \quad \eta=0.15.
\]

Objective accuracy against the reference objective is computed only after selection for reporting. The key design choice is that structural correctness contributes explicitly to candidate selection, while ground-truth objective values are not used to choose candidates in the formal experimental setting.

### 4.6 Baseline and Method Variants

We implement six methods.

**Direct.** Selects the first candidate for each problem without using execution or structure verification for ranking. This simulates a single-shot LLM generation setting.

**Best-of-K.** Given K candidates, selects the first executable candidate in candidate order. If no candidate is executable, it selects the first candidate. This baseline uses execution only as a coarse filter.

**Solver-Filter.** Selects candidates using solver-only signals observed from the candidate itself. Executable candidates are preferred, `Optimal` status is preferred, and candidates that return an objective value are preferred. It does not use reference objective values or LP structure scores.

**Structure-Only.** Selects candidates using only LP structure completeness. This variant tests whether domain-specific structure rules provide useful supervision independent of final objective values.

**Generic-LP-Statistics.** A hallucination-detection-style baseline can use generic LP statistics, such as number of variables, number of constraints, binary-variable count, and whether an objective exists, without replenishment-specific rules. This baseline helps distinguish domain-specific structure checking from generic artifact inspection.

**ReplenishVerifier-Full.** Uses the full ground-truth-free score combining execution, solver optimality, LP structure completeness, and semantic consistency.

**ReplenishVerifier-Repair.** Uses the same selection criterion as the full method but additionally generates repair prompts when expected structures are missing. The current implementation does not call a real LLM for repair; instead, it saves prompts for future use with models such as Qwen or Claude.

### 4.7 Repair Feedback Generation

When a candidate misses required structures, ReplenishVerifier converts missing labels into natural language feedback. For example:

- “你的模型缺少库存平衡约束。应加入类似 \(I[t] = I[t-1] + Q[t] - demand[t]\) 的约束。”
- “题目包含固定订货成本，但你的模型没有 binary 订货变量 \(Y[t]\)。”
- “你的模型有订货变量 \(Q[t]\)，但没有 Big-M 约束 \(Q[t] \le M \cdot Y[t]\)。”

These messages provide process-level supervision and can be used for self-repair, DPO data construction, or model debugging.

---

## 5. Experiments

### 5.1 Research Questions

The experiments are designed to answer the following questions:

**RQ1.** Does LP-structure-grounded verification improve candidate selection compared with Direct, Best-of-K, and Solver-Filter baselines?

**RQ2.** Does structure-only verification provide useful signal independent of solver objective values?

**RQ3.** Which components of ReplenishVerifier contribute most to performance: solver execution, objective matching, LP structure verification, or repair feedback?

**RQ4.** How does performance change under low-resource candidate budgets, such as \(K=1,2,4,8\)?

**RQ5.** How do methods behave across different difficulty levels and replenishment problem types?

### 5.2 Dataset

We evaluate on the ReplenishmentOR-style benchmark generated by the proposed template pipeline. The benchmark contains five problem types described in Section 4.2.

Planned dataset statistics are reported in Table 1. Actual values should be filled after final benchmark generation.

| Split / Type | #Instances | Difficulty | Main Structures |
|---|---:|---|---|
| single_period_newsvendor | `[TO FILL]` | easy | order, inventory, shortage, holding cost, shortage cost |
| single_item_multi_period | `[TO FILL]` | medium | inventory balance, order, inventory, holding cost |
| single_item_multi_period_shortage | `[TO FILL]` | medium | inventory balance, shortage, shortage cost |
| multi_item_capacity | `[TO FILL]` | hard | multi-item inventory balance, capacity |
| fixed_order_cost_big_m | `[TO FILL]` | hard/expert | binary order variable, fixed cost, Big-M |
| Total | `[TO FILL]` | — | — |

`[TO FILL: final benchmark generation configuration, random seed, train/validation/test split if used]`

### 5.3 Candidate Generation

For each problem, an LLM generates \(K\) candidate solver programs. The current codebase does not depend on a real LLM; it consumes candidate JSONL files with the following schema:

```json
{
  "problem_id": "string",
  "candidate_id": "string",
  "method": "direct|cot|best_of_k|repair",
  "generated_text": "string",
  "generated_code": "string"
}
```

Candidate generation models planned for evaluation include:

- `[TO FILL: model name, e.g., Qwen2.5-3B]`
- `[TO FILL: model name, e.g., Qwen2.5-7B]`
- `[TO FILL: model name, e.g., Qwen3-8B]`

`[TO FILL: decoding temperature, top-p, max tokens, number of candidates K]`

### 5.4 Compared Methods

We compare the six methods implemented in the codebase:

| Method | Execution Used | Reference Objective Used for Selection | Replenishment LP Structure Used | Repair Feedback |
|---|---:|---:|---:|---:|
| Direct | No ranking | No | No | No |
| Best-of-K | Yes | No | No | No |
| Solver-Filter | Yes | No | No | No |
| Generic-LP-Statistics | Yes | No | No; generic LP statistics only | No |
| Structure-Only | Required for LP export | No | Yes | No |
| ReplenishVerifier-Full | Yes | No | Yes | No |
| ReplenishVerifier-Repair | Yes | No | Yes | Yes |

### 5.5 Metrics

We report:

1. Executable Rate;
2. Optimal Rate;
3. Objective Accuracy;
4. Structure Completeness;
5. Inventory Balance Accuracy;
6. Constraint Coverage;
7. Average Runtime;
8. Average Repair Feedback Count.

All metrics are computed per selected candidate and then averaged over the evaluation set. Constraint coverage is computed over expected structures only.

### 5.6 Main Results

Table 2 reports the main comparison.

| Method | Executable Rate | Optimal Rate | Objective Accuracy | Structure Completeness | Constraint Coverage | Avg. Runtime |
|---|---:|---:|---:|---:|---:|---:|
| Direct | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| Best-of-K | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| Solver-Filter | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| Structure-Only | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| ReplenishVerifier-Repair | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |

Expected interpretation: if LP structure verification is effective, ReplenishVerifier-Full should improve structure completeness and constraint coverage over Direct, Best-of-K, and Solver-Filter. Solver-Filter may improve objective accuracy but can still select structurally invalid models. Structure-Only may improve structural metrics but may not always optimize objective accuracy.

`[TO FILL: actual main result analysis]`

### 5.7 Ablation Study

The ablation study isolates the effect of each supervision component.

| Variant | Removed Component | Objective Accuracy | Structure Completeness | Constraint Coverage |
|---|---|---:|---:|---:|
| ReplenishVerifier-Full | None | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| Solver-Filter | LP structure verification | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| Structure-Only | Solver objective signal | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| ReplenishVerifier-Repair | No actual second-round LLM repair yet | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |

The main hypothesis is that removing LP structure verification will reduce structure completeness and constraint coverage, while removing solver-status signals may reduce executability or optimality. Objective accuracy is reported only after candidate selection and is not used as a formal selection signal. This ablation is intended to demonstrate that solver feedback and LP-structure feedback are complementary.

`[TO FILL: actual ablation result analysis]`

### 5.8 Low-Resource K Analysis

To evaluate candidate budget efficiency, we vary the number of candidates per problem:

\[
K \in \{1,2,4,8\}.
\]

| K | Method | Objective Accuracy | Structure Completeness | Constraint Coverage | Avg. Runtime |
|---:|---|---:|---:|---:|---:|
| 1 | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| 2 | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| 4 | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| 8 | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |

Expected interpretation: increasing K should improve candidate selection quality but also increase runtime. A practically useful result would be that moderate K, such as K=4, achieves most of the gain of K=8.

`[TO FILL: actual K analysis]`

### 5.9 Difficulty-wise Results

We further group results by difficulty.

| Difficulty | Method | Objective Accuracy | Structure Completeness | Inventory Balance Accuracy | Constraint Coverage |
|---|---|---:|---:|---:|---:|
| easy | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| medium | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| hard | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |
| expert | ReplenishVerifier-Full | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` | `[TO FILL]` |

Expected interpretation: harder problems contain more structures and therefore provide more opportunities for structure-aware verification to outperform result-only baselines.

`[TO FILL: actual difficulty-wise analysis]`

---

## 6. Analysis

### 6.1 Why Result-Level Verification Is Insufficient

A key motivation of ReplenishVerifier is that solver execution can validate the generated mathematical program but not necessarily the intended problem semantics. Consider a multi-period replenishment task. A candidate model may introduce order variables and minimize ordering cost, but omit inventory variables and inventory balance constraints. Such a model may still be feasible and optimal, but it is not a replenishment model in the intended sense.

Similarly, a fixed-order-cost model may include fixed costs in the objective but fail to use binary order-trigger variables. Alternatively, it may introduce binary variables but omit Big-M constraints linking order quantities to those binaries. In these cases, objective values may accidentally match small test instances, but the model structure remains wrong and will fail under distribution shift.

LP-structure-grounded verification addresses this by inspecting the model artifact itself. It asks not only “did the solver return an answer?” but also “does the generated LP contain the structures required by this replenishment problem?”

### 6.2 Solver Feedback and Structure Feedback Are Complementary

Solver feedback and structure feedback capture different failure modes. Solver feedback is good at detecting syntax errors, runtime errors, infeasibility, and objective mismatch. Structure feedback is good at detecting missing semantic components. Therefore, a full scoring function should combine both.

The expected ablation pattern is:

- Solver-Filter improves executable and objective-related metrics but may not improve structure completeness.
- Structure-Only improves structure completeness but may select models with poor objective values.
- ReplenishVerifier-Full balances both and should perform best on combined metrics.

`[TO FILL: empirical evidence from ablation]`

### 6.3 Repair Feedback as Process Supervision

The repair module converts missing structures into actionable natural language feedback. This is useful for three reasons.

First, it makes verification interpretable. Instead of returning a scalar score only, the system explains which modeling structures are missing.

Second, it enables self-repair. A generated candidate and its feedback can be passed back to an LLM to produce a revised candidate.

Third, it supports preference-data construction. Candidates with higher structure-grounded scores can be treated as chosen responses, while candidates with missing structures can be treated as rejected responses.

The current implementation generates repair prompts but does not yet call a real LLM for second-round repair. This design keeps the CPU-only experimental pipeline reproducible and separates verifier evaluation from LLM inference.

### 6.4 Error Categories

We plan to analyze errors using the following categories:

1. **Execution errors:** syntax errors, missing imports, invalid solver API usage.
2. **Solver errors:** infeasible, unbounded, or no valid objective.
3. **Objective errors:** executable and structurally plausible models with incorrect objective values.
4. **Missing variable errors:** missing \(Q\), \(I\), \(B\), or \(Y\).
5. **Missing constraint errors:** missing inventory balance, capacity, or Big-M constraints.
6. **Cost-term errors:** missing holding, shortage, or fixed ordering costs.
7. **Over-modeling errors:** extra structures not required by the problem.

`[TO FILL: error distribution table]`

---

## 7. Limitations

This work has several limitations.

First, the current LP parser is a lightweight parser targeting PuLP-exported LP files. It is not a complete parser for all LP/MPS dialects. Extending the parser to support more solver backends and more complex algebraic forms is important future work.

Second, the current structure rules rely partly on naming conventions such as `Q`, `I`, `B`, and `Y`. This is acceptable for controlled benchmark generation and candidate evaluation with instructed naming, but real-world LLM outputs may use different variable names. Future work should combine name-based rules with graph-based or coefficient-pattern-based LP analysis.

Third, the benchmark currently focuses on five replenishment problem families. More realistic industrial settings may include stochastic demand, service-level constraints, lost sales, multi-echelon networks, supplier constraints, transportation costs, lead times, and rolling-horizon policies.

Fourth, ReplenishVerifier-Repair currently generates repair prompts but does not perform actual LLM-based repair in the CPU-only prototype. Future experiments should evaluate second-round repaired candidates generated by LLMs.

Fifth, the full scoring function uses manually chosen weights. Although these weights are interpretable, future work could learn them from validation data or replace them with a trained process reward model. We do not claim these weights are universally optimal.

Finally, structure correctness does not guarantee complete mathematical correctness. A model may contain all expected high-level structures but still use wrong coefficients, wrong indices, or incorrect boundary conditions. Fine-grained coefficient and index verification remains an important direction. Therefore, ReplenishVerifier should be understood as a complementary structural supervision signal, not as a complete replacement for solver validation, objective evaluation, hallucination auditing, or model repair systems.

---

## 8. Conclusion

We presented ReplenishVerifier, an LP-structure-grounded process supervision framework for LLM-based replenishment optimization modeling. Unlike result-only verification methods that rely mainly on executability, solver status, objective values, or candidate agreement, ReplenishVerifier inspects the LP structure induced by generated solver code and verifies inventory-specific modeling semantics. The framework parses LP files, checks structures such as inventory balance, order variables, shortage variables, capacity constraints, binary order triggers, fixed ordering costs, and Big-M constraints, and produces structure-grounded scores and repair feedback.

The proposed framework reframes verification for LLM-based OR modeling from final-answer checking to domain-structured process supervision. By focusing on inventory replenishment, it demonstrates how domain knowledge can be operationalized as verifiable LP-level rules. The experimental protocol includes main comparisons, ablations, low-resource K analysis, and difficulty-wise evaluation. Full empirical results will be filled after running LLM candidate generation and benchmark-scale experiments. `[TO FILL: final result summary]`

In future work, we plan to extend ReplenishVerifier to richer replenishment settings, support more solver backends, improve variable-name-agnostic LP analysis, and use structure-grounded scores to construct DPO or other preference-learning datasets for low-resource adaptation of optimization-modeling LLMs.

---

## Appendix A. Implementation Notes

The current project implements the following commands:

```bash
python scripts/generate_benchmark.py \
  --output data/benchmark.jsonl \
  --lp-dir outputs/reference_lp \
  --n-per-type 20 \
  --seed 42

python -m replenish.experiments.run_all_methods \
  --benchmark data/benchmark.jsonl \
  --candidates data/candidates/example_candidates.jsonl \
  --out_dir runs/exp_demo \
  --k_values 1,2,4,8 \
  --timeout 30

python -m replenish.experiments.build_paper_tables \
  --exp_dir runs/exp_demo \
  --out_dir runs/paper_tables
```

The table builder produces:

```text
runs/paper_tables/table1_benchmark.md
runs/paper_tables/table2_main.md
runs/paper_tables/table3_ablation.md
runs/paper_tables/table4_low_resource.md
runs/paper_tables/table5_difficulty.md
```
